node_modules/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

*.wav
*.mp3
*.flac
recordings/

# Whisper.cpp ggml model files. Downloaded locally per user, ~150 MB - 3 GB
# each. Same "no binary deps in source" stance as recordings/. Tracking a
# README.md or similar under models/ is still possible.
models/*.bin

# Local-only binary install artifacts (e.g. whisper.cpp release zips, future
# GPU runtimes, model archives). The project does not vendor binary deps.
*.zip

coverage/

# Smoke-test scratch files (server stdout/stderr captures, etc).
tmp-*

.DS_Store
Thumbs.db
*.swp
*.swo
.vscode/
.idea/
